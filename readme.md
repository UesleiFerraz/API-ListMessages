# comandos subir codigo

git init
git add .
git remote add origin "your link"
git commit -m "msg"

## primeira vez push
set upstream
## segunda vez
git push

# comandos clone

git clone "your link"
git pull